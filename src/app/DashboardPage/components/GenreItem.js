import React from 'react';

const GenreItem = ({ genre }) => {
    return (
        <button genreId={this.genre.id} genreName={this.genre.name}>{this.genre.name}</button>
    );
};

export default GenreItem;